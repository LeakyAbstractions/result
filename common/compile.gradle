// Configure java compiler
sourceCompatibility = System.getenv('JDK_VERSION') ?: '8'
targetCompatibility = System.getenv('JDK_VERSION') ?: '8'

tasks.withType(JavaCompile).all {
    options.compilerArgs.addAll(['-Xlint:all', '-Werror'])
}
