// Configure source code formatting
spotless {
    java {
        eclipse().configFile rootProject.file('common/.formatting.xml')
        importOrderFile rootProject.file('common/.importorder')
        removeUnusedImports()
    }
    groovyGradle {
        greclipse().configFile rootProject.file('common/.formatting.xml')
        importOrderFile rootProject.file('common/.importorder')
    }
}
